import { HasActivityPipe } from './has-activity.pipe';

describe('HasActivityPipe', () => {
  it('create an instance', () => {
    const pipe = new HasActivityPipe();
    expect(pipe).toBeTruthy();
  });
});
